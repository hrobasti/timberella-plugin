# Timberella configuration file

#################################################
# General
#################################################

# language: messages locale file to load from /lang (default: en_US)
language: 'en_US'

# Text displayed inside the colored chat prefix brackets.
chat-prefix-label: 'Timberella'

# Display the startup banner in console when the plugin enables.
startup-banner-enabled: true

# Config watcher toggle (auto-reload & merge every ~5s if enabled)
config-watch-enabled: true

# Interval (seconds) between watcher polls, minimum 1 second
config-watch-interval-seconds: 5

# Enable anonymous usage metrics via bStats (https://bstats.org)
metrics-enabled: true

#################################################
# Update mechanics
#################################################

update-check:
  # Master switch controlling Modrinth/Hangar polling.
  enabled: true
  # 0 = Modrinth + Hangar, 1 = only Modrinth, 2 = only Hangar
  provider: 0
  # Interval (in hours) for automatic update checks (minimum 1)
  interval-hours: 24
  # Notify about pre-release builds (beta/alpha). Requires support from provider APIs.
  include-prereleases: false
  # Prefer builds that match the server's Minecraft version.
  filter-by-server-version: true
  # Log update availability to console when a newer version is found.
  notify-console: true
  # Always show provider summaries even when no update is available.
  notify-console-always-shown: false
  # Notify ops on join when an update is available.
  notify-op-join: true

#################################################
# Modules
#################################################

# Enable timber ability
enable-timber: true

# Enable replanting of saplings after tree felling
enable-replant: true

# Enable leaves decay after tree felling
# if true, leaves around felled logs will start decaying, netherless of enable_timber and enable_replant
# doesn't require timber permission to function - it's a global setting
enable-leaves-decay: true

#################################################
# Timber settings
#################################################

# When should trees be felled automatically?
# 0 = only when sneaking
# 1 = only when NOT sneaking
# 2 = sneaking or not (always)
sneak-mode: 0

# Tree connection detection:
# true = consider diagonal neighbors, false = only 6 orthogonal faces
include-diagonals: true

# Interval (in ticks) between breaking subsequent blocks when felling sequentially
break-interval-ticks: 2

tools:
  # Which axes are allowed to trigger tree felling

  allowed-axes:
    - STONE_AXE
    - COPPER_AXE
    - IRON_AXE
    - GOLDEN_AXE
    - DIAMOND_AXE
    - NETHERITE_AXE
  
  # Minimum remaining durability required before felling starts (protection)
  min-remaining-durability: 10

  # Durability mode: "first" counts only the first block / "all" counts all broken blocks
  durability-mode: all

  # Multiplier for durability cost when mode is "all" (total_cost = round(broken_blocks * multiplier))
  durability-multiplier: 0.5

# Maximum number of log blocks to break in one go (safety)
max-blocks: 1024

# Species-specific safety limits (radius = Chebyshev X/Z distance; vertical = |Y| offset)
species-limits:
  mangrove:
    enabled: true
    max-blocks: 128
    max-horizontal-radius: 9
    max-vertical-radius: 36
  jungle:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 8
    max-vertical-radius: 32
  spruce:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 6
    max-vertical-radius: 32
  oak:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 6
    max-vertical-radius: 24
  pale-oak:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 5
    max-vertical-radius: 16
  dark-oak:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 6
    max-vertical-radius: 12
  birch:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 2
    max-vertical-radius: 12
  acacia:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 8
    max-vertical-radius: 12
  cherry:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 9
    max-vertical-radius: 12
  mushroom-brown:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 4
    max-vertical-radius: 12
  mushroom-red:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 2
    max-vertical-radius: 12
  warped:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 6
    max-vertical-radius: 32
  crimson:
    enabled: true
    max-blocks: -1
    max-horizontal-radius: 6
    max-vertical-radius: 32

# Categories with per-material toggles (set true/false). Unknown materials are ignored safely.
categories:
  logs:
    OAK_LOG: true
    PALE_OAK_LOG: true
    SPRUCE_LOG: true
    BIRCH_LOG: true
    JUNGLE_LOG: true
    ACACIA_LOG: true
    DARK_OAK_LOG: true
    MANGROVE_LOG: true
    MANGROVE_ROOTS: true
    CHERRY_LOG: true
    CRIMSON_STEM: true
    WARPED_STEM: true
    MUSHROOM_STEM: true
    BROWN_MUSHROOM_BLOCK: true
    RED_MUSHROOM_BLOCK: true
  stripped-logs:
    STRIPPED_OAK_LOG: false
    STRIPPED_PALE_OAK_LOG: false
    STRIPPED_SPRUCE_LOG: false
    STRIPPED_BIRCH_LOG: false
    STRIPPED_JUNGLE_LOG: false
    STRIPPED_ACACIA_LOG: false
    STRIPPED_DARK_OAK_LOG: false
    STRIPPED_MANGROVE_LOG: false
    STRIPPED_CHERRY_LOG: false
    STRIPPED_CRIMSON_STEM: false
    STRIPPED_WARPED_STEM: false
  woods:
    OAK_WOOD: false
    PALE_OAK_WOOD: false
    SPRUCE_WOOD: false
    BIRCH_WOOD: false
    JUNGLE_WOOD: false
    ACACIA_WOOD: false
    DARK_OAK_WOOD: false
    MANGROVE_WOOD: false
    CHERRY_WOOD: false
  stripped-woods:
    STRIPPED_OAK_WOOD: false
    STRIPPED_PALE_OAK_WOOD: false
    STRIPPED_SPRUCE_WOOD: false
    STRIPPED_BIRCH_WOOD: false
    STRIPPED_JUNGLE_WOOD: false
    STRIPPED_ACACIA_WOOD: false
    STRIPPED_DARK_OAK_WOOD: false
    STRIPPED_MANGROVE_WOOD: false
    STRIPPED_CHERRY_WOOD: false
  fences:
    OAK_FENCE: false
    SPRUCE_FENCE: false
    BIRCH_FENCE: false
    JUNGLE_FENCE: false
    ACACIA_FENCE: false
    DARK_OAK_FENCE: false
    MANGROVE_FENCE: false
    CHERRY_FENCE: false
    CRIMSON_FENCE: false
    WARPED_FENCE: false
  additions:
    BEE_NEST: true
    BEEHIVE: true
    CREAKING_HEART: true

#################################################
# Replant settings
#################################################

replant:
  # List of saplings to replant (must match item IDs)
  saplings:
    - OAK_SAPLING
    - PALE_OAK_SAPLING
    - SPRUCE_SAPLING
    - BIRCH_SAPLING
    - JUNGLE_SAPLING
    - ACACIA_SAPLING
    - DARK_OAK_SAPLING
    - CHERRY_SAPLING
    - MANGROVE_PROPAGULE

#################################################
# Leaves decay settings
#################################################

leaves-decay:
  # Search radius around felled logs used to discover candidate leaves (flood-fill depth)
  # Minimum accepted value: 0 (values below are clamped)
  decay-radius: 5

  # Hard safety buffer: leaves farther than this (3D distance) from every felled log stay untouched
  # Minimum accepted value: 1
  max-distance: 4

  # Interval (in ticks) between leaf removal batches
  # Minimum accepted value: 1 tick
  batch-interval-ticks: 2

  # Maximum number of leaves removed per batch
  # Minimum accepted value: 1
  batch-size: 20
